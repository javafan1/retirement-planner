package com.daviddunn.retirementplanner.domain.socialsecurity.analysis;

/** Independent ranking measures for mortality-weighted claiming strategies. */
public enum SocialSecurityExpectedValueMeasure {
    EXPECTED_NOMINAL,
    EXPECTED_REAL,
    EXPECTED_PRESENT_VALUE
}
