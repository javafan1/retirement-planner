package com.daviddunn.retirementplanner.app.socialsecurity;

/** Deliberately separate from deterministic integrated ranking measures. */
public enum LongevityWeightedComparisonObjective {
    EXPECTED_PV_AFTER_TAX_ESTATE
}
